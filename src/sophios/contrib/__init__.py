"""Contrib zone: peripheral surfaces (ICT, Workflow Builder, REST).

Supported and kept working, but deliberately outside the core compiler.
contrib may import core; core may never import contrib. See
design_docs/core-refactor-design.md, Spec 0.
"""
